# IDE / éditeurs
.idea/

# Dépendances PHP (installées par Composer)
vendor/

# Fichiers de configuration ou sensibles
.env
.env.local
.env.*.local
ohayoo.sql

# Fichiers de log / cache
*.log
*.cache
*.tmp
*.swp

# Dossiers d’uploads (pour éviter de versionner les fichiers utilisateur)
upload/chat/*
upload/post/*
upload/profile/*
upload/users/*
upload/cover/*

# Si jamais tu veux garder les dossiers vides dans Git
!upload/chat/.gitkeep
!upload/post/.gitkeep
!upload/profile/.gitkeep
!upload/users/.gitkeep
!upload/cover/.gitkeep

# Système
.DS_Store
Thumbs.db

# Node (si tu utilises npm/yarn pour le frontend)
node_modules/
package-lock.json
npm-debug.log*
